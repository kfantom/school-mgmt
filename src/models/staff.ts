export class staff {
    public id: number;
    public name: string;
    public section: string;
    constructor(id: number, name: string, section: string) {
        this.id = id;
        this.name = name;
        this.section = section;
    }
}